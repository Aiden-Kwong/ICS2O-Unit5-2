// This checks for if the button is clicked and if it is, it triggers the function called message.
document.getElementById('button').addEventListener('click', message)

// This sends the alert popup when the function is triggered (by the button).
function message () {
  alert('Button was clicked')
}
